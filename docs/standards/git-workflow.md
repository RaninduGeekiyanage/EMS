# Git Workflow

- Main branch: `main` (production-ready).
- Development branch: `develop`.
- Feature branches: `feature/M01-employee-master`, `feature/M02-biometric-adapter`.
- Commit format: `[Module] type: description` (e.g. `[M01] feat: add employee payment mode`).
