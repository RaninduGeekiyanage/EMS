# TypeScript & React Conventions

## 1. Strict Compiler Rules
- No `any` types permitted. Declare full TypeScript interfaces in `resources/js/Types/`.

## 2. Component Organization
- Inertia pages in `resources/js/Pages/`.
- Reusable UI elements in `resources/js/Components/` following ShadCN conventions.
